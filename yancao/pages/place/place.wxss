.title {
  position: relative;
  text-align: left;
}
.items .choice {
  position: relative;
  text-align: right;
}
.begin {
  position: absolute;
  left: 50%;
  top: 60%;
  transform: translateX(-50%);
}
